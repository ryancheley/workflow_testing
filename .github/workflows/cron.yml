on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '*/15 * * * *'

jobs:
  say_hello_cron:
    runs-on: ubuntu-18.04
    steps:
    - run: |
        echo "This is a test of Cron"
